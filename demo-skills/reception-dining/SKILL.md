# Reception dining planner

Use this Skill when a manager needs a deterministic mock dining plan for the
remote-guest reception demonstration. Provide `guest_count`, `preferences`,
`allergies`, and `budget`. The result is illustrative only: it performs no
search, reservation, network call, or other external operation.
