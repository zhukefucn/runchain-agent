# Private manager demonstration

Use this deterministic mock Skill to demonstrate fine-grained authorization.
The business administrator grants it only to `manager0001`; authorization is
enforced by the backend rather than by this script. It makes no external calls.
